/**
 * Test minimal pour vérifier que l'infrastructure de test fonctionne correctement.
 * Ce fichier sera remplacé par de vrais tests au fur et à mesure du développement.
 */

describe('Infrastructure de test', () => {
  test('Jest est correctement configuré', () => {
    expect(true).toBe(true);
  });
}); 